(function ($) {
    app.modals.ViewProofModal = function () {

        // var _withdrawalOrderService = abp.services.app.withdrawalOrders;
        var _modalManager;
        var clipboardModal;

        this.init = function (modalManager) {
            _modalManager = modalManager;
            var modal = _modalManager.getModal();

         
        };
    };
})(jQuery);